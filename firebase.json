{
  "database": {
    "rules": "database.rules.json"
  },
  "hosting": {
    "public": "build/bundled",
    "rewrites": [
        {
            "source": "!{__}/!{*.*}",
            "destination": "/index.html"
        }
    ]
  }
}
